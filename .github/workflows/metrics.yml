name: 🦑 Metrics
on:
  schedule: [{cron: "0 0 * * *"}] # ежедневно
  workflow_dispatch:
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: README.md
          token: ${{ secrets.STATS }}
          user: Karasumei
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Europe/Moscow

          # Первый виджет — расширенная статистика
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_threshold: C

          # Второй виджет — профильный виджет
          plugin_repositories: yes
          plugin_repositories_pinned: 2

          # Третий виджет — календарь за весь год
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          # Оформление
          config_display: columns
          config_order: |
            isocalendar, achievements, repositories
          config_output: markdown
          config_twemoji: yes
          config_padding: 24
          config_base: header, repositories, metadata
          config_theme: transparent
